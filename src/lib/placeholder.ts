/**
 * PLACEHOLDER contact details.
 *
 * The design prototype shipped with invented corporate facts (a founding
 * year, an address, phone numbers, e-mail addresses). Those belonged to the
 * fictional brand in the mockup and are deliberately NOT carried over.
 * Every value below is an obvious, language-neutral placeholder and must be
 * replaced with real data before launch — tracked in MANUEL.md.
 */
export const placeholderContact = {
  addressLines: ["[ADDRESS LINE 1]", "[POSTCODE / CITY]"],
  phone: "[PHONE]",
  email: "[EMAIL]",
} as const;
